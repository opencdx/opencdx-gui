apiVersion: 1

providers:
  - name: 'Spring Boot Dashboard'
    folder: 'Services'  # Optional: Specify a folder to organize dashboards
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: true